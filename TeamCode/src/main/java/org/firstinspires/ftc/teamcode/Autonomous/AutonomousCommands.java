package org.firstinspires.ftc.teamcode.Autonomous;
import static java.lang.Thread.sleep;

import com.qualcomm.robotcore.hardware.DcMotor;

    /*
public class AutonomousCommands {


    private final DcMotor right_Drive;
    private final DcMotor left_Drive;

    private static void encoderDrive(double v, int i, int i1, double v1) {
    }

    // Constructor to initialize motors


    public AutonomousCommands(DcMotor right_Drive, DcMotor left_Drive) {
        this.right_Drive = right_Drive;
        this.left_Drive = left_Drive;
    }

    public void turnLeft90() throws InterruptedException {
        // Adjust power and time based on your robot's characteristics
        right_Drive.setPower(0);
        left_Drive.setPower(-0.5);
        // Adjust the sleep time based on experimentation
        sleep(1000);
        stopMotors();
    }

    public void stopMotors() {
        right_Drive.setPower(0);
        left_Drive.setPower(0);
    }


    public void forward1ft() {
        AutonomousCommands.encoderDrive(0.5, 12, 12, 5.0);
    }
}
     */
